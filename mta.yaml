_schema-version: "3.2"
ID: dmsuiapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: dmsuiapp-destination-content
  type: com.sap.application.content
  requires:
  - name: dmsuiapp-destination-service
    parameters:
      content-target: true
  - name: dmsuiapp-repo-host
    parameters:
      service-key:
        name: dmsuiapp-repo-host-key
  - name: dmsuiapp-uaa
    parameters:
      service-key:
        name: dmsuiapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: dmsuiapp_html_repo_host
          ServiceInstanceName: dmsuiapp-html5-service
          ServiceKeyName: dmsuiapp-repo-host-key
          sap.cloud.service: dmsuiapp
        - Authentication: OAuth2UserTokenExchange
          Name: dmsuiapp_uaa
          ServiceInstanceName: dmsuiapp-xsuaa-service
          ServiceKeyName: dmsuiapp-uaa-key
          sap.cloud.service: dmsuiapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: dmsuiapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: dmsuiapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - dmsuiapp.zip
      name: dmsuiapp
      target-path: resources/
- name: dmsuiapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: dmsuiapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: dmsuiapp-destination-service
    service-plan: lite
- name: dmsuiapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: dmsuiapp-xsuaa-service
    service-plan: application
- name: dmsuiapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: dmsuiapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
